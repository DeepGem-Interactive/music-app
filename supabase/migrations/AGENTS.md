# Database Migrations

SQL files for schema changes. **These do NOT auto-run.**

Run manually in Supabase SQL Editor: Dashboard → SQL Editor → paste contents → Run.

Never edit existing migrations that have been applied - create new ones instead.
